name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://resumildo-bot.onrender.com)
          echo "Status: $STATUS"
          echo "Status: $STATUS"
